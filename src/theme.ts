import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  fontSizes: {
    sm: "12px",
    md: "16px",
    lg: "20px",
    xl: "24px",
    xxl: "72px",
  },
});
export default theme;
